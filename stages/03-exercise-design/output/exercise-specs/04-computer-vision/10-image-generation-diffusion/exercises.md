# Exercises — Image Generation â€” Diffusion Models

## Exercises

1. **Implement** the iterative forward diffusion process on a synthetic 4×4 image tensor of ones. Use T=100 timesteps with a linear β schedule from 0.0001 to 0.02. At each step, compute x_t from x_{t-1} per the update rule. Print the Signal-to-Noise Ratio (mean pixel magnitude squared divided by noise variance) at timesteps 0, 25, 50, 75, and 100. Confirm that SNR monotonically decreases. Set torch.manual_seed(42) before generating noise so your output is reproducible and checkable.

2. **Reimplement** the noising using the closed-form q(x_t | x_0) with ᾱ_t computed as the cumulative product. Sample x_t directly at the same five timesteps used in Exercise 1, again with torch.manual_seed(42). Compute the element-wise absolute difference between the iterative result from Exercise 1 and the closed-form result at each timestep. Print the maximum difference across all timesteps — it should be near machine epsilon (effectively zero), confirming the two formulations are mathematically equivalent.

3. **Install** the `diffusers` library (`pip install diffusers torch`). Load `stabilityai/stable-diffusion-2-base` with `DDPMScheduler` (default 1000 steps) and `DDIMScheduler` with `num_inference_timesteps` set to 50. Generate the same prompt ("a watercolor painting of a mountain lake at sunrise, soft brush strokes") with both schedulers using the same seed (torch.manual_seed(0)). Print the wall-clock time for each, save both images as `outputs/sd-ddpm.png` and `outputs/sd-ddim.png`, and compute the per-pixel mean absolute difference between the two outputs. Report time ratio and pixel divergence in a single printed summary line.

4. **Call** the Stability AI REST API (`https://api.stability.ai/v2beta/stable-image/generation/core`) using `requests` with an API key from the `STABILITY_API_KEY` environment variable. Parameterize the prompt using a Python dict representing a GTM campaign brief — fields: `product_name`, `target_persona`, `value_prop`, `tone`. Construct the prompt string by interpolating these fields into a template (e.g., "A {tone} advertisement image for {product_name}, targeting {target_persona}, highlighting: {value_prop}"). Send the request, validate that the response status is 200 and the content-type is `image/png`, then write the bytes to `outputs/gtm-ad-{product_name}.png`. If validation fails, print the error payload and exit non-zero. Test with two different campaign briefs and confirm both files land on disk with size > 0.

5. **Build** a comparison harness that demonstrates latent-space dimensionality reduction. Load any 512×512×3 image (use `PIL.Image` with a solid color or gradient if no source image is available). Instantiate the VAE encoder from `stabilityai/stable-diffusion-2-base` (`AutoencoderKL.from_pretrained(..., subfolder="vae")`). Encode the image to latent space and print: (a) the input tensor shape (C×H×W), (b) the latent tensor shape, (c) the total element count for both, and (d) the compression ratio (input elements / latent elements). Save the encoded latent tensor and a decoded reconstruction to `outputs/vae-latent-shape.json` (containing the four printed values as a JSON object) and `outputs/vae-reconstruction.png`. Confirm the reconstruction is visually similar to the input despite the compression.

6. **Design** and implement a `LatentDiffusionCostModel` class that estimates FLOPs for one full denoising pass in pixel space (operating on a 512×512×3 tensor with a standard U-Net) versus latent space (operating on a 64×64×4 latent with the same U-Net architecture scaled down). Assume each U-Net layer processes a feature map of the given spatial resolution with 256 channels and a 3×3 conv kernel; estimate FLOPs as `2 * H * W * C_in * C_out * kernel^2` per conv layer, and assume 20 downsampling/upsampling conv layers total. Compute the estimated FLOPs for both configurations, print the ratio, and write a one-paragraph analysis to `outputs/skill-latent-diffusion-cost.md` explaining whether the theoretical speedup matches the empirical timing difference you observed in Exercise 3, and if not, what factors (attention layers, VAE overhead, memory bandwidth) account for the gap.
