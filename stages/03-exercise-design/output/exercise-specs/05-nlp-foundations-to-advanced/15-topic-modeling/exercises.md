# Exercises — Topic Modeling â€” LDA and BERTopic

## Exercises

1. **Implement LDA on a corpus and inspect topic-word distributions.** Load the 20 Newsgroups subset used in the lesson (or a CSV of ~500 short product reviews). Fit `sklearn.decomposition.LatentDirichletAllocation` with `n_components=8` and default priors. Print the top 10 words for each topic, sorted by weight. Then set `doc_topic_prior=0.001` (sparser α), refit, and print the top 10 words again. Observe in the terminal how the word lists sharpen or collapse. State in a comment which α produced more interpretable topics for your corpus.

2. **Implement BERTopic on the same corpus and compare its topics to LDA.** Run `BERTopic().fit_transform(documents)` on the same documents from Exercise 1. Print the top 8 topics with their top-10 terms using `topic_model.get_topic_info()` and `get_topic()`. Pick one LDA topic from Exercise 1 and find the closest BERTopic topic by term overlap. Print both topic's top words side by side with a one-line note on what each model surfaced that the other did not.

3. **Compute coherence scores across multiple K values and select the best model.** Train LDA with `n_components` ∈ {5, 10, 15, 20, 25} on the corpus from Exercise 1. For each model, compute the c_v coherence score using `gensim.models.CoherenceModel`. Print a table: K, coherence score, top-5 words of the dominant topic. Identify the K with the highest coherence, then apply an interpretability heuristic (can you name each topic in one phrase?) to decide whether to ship that K or the next-best. Print your final choice and justification to the terminal.

4. **Configure BERTopic pipeline components and trace their effect on output.** Build three BERTopic variants on the same corpus: (a) default pipeline, (b) UMAP with `n_neighbors=5` and `min_dist=0.0`, (c) HDBSCAN with `min_cluster_size=3` and `min_samples=1`. Fit each variant. For each, print: number of topics discovered, number of outlier documents, and the top-5 words of the largest topic. Add a comment explaining which component change had the biggest effect on topic granularity and why.

5. **Apply topic modeling to lost-deal notes from a CRM export and extract actionable themes.** Load a CSV of lost-deal notes (e.g., exported from HubSpot or Salesforce — column `loss_reason_notes`). Run BERTopic on the notes text. For each of the top 5 topics, manually label it with a one-sentence actionable theme (e.g., "Pricing 15–20% above competitor quote," "Missing SOC 2 documentation," "Champion left company before close"). Write a reusable script that prints each topic ID, its top-8 terms, the count of deals assigned to it, and your action label. Save the script to `signals/examples/lost_deal_topics.py` and run it — terminal output must show the labeled topics.

6. **Design an end-to-end topic-modeling workflow on support tickets and produce an analysis artifact.** Obtain a corpus of customer support tickets (Zendesk CSV export, Intercom export, or the provided sample dataset of 1,000 ticket bodies). Choose between LDA and BERTopic — justify the choice in writing based on corpus size, ticket length, and whether you need semantic clustering. Run the chosen pipeline, compute coherence or use the interpretability heuristic, and label the top 5 topics. For each topic, propose a concrete GTM action (product fix, self-serve doc, escalation routing rule, CS training module). Write the full analysis — model choice rationale, topic table with labels, and recommended actions — to `outputs
