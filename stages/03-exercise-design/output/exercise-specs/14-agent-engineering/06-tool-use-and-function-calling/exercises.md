# Exercises — Tool Use and Function Calling

## Exercises

1. **Define a tool schema and invoke it.** Write a Python script that defines a `search_prospects` function using JSON Schema (parameters: `company_domain` string, `title_filter` optional string). Pass this tool definition to the OpenAI or Anthropic API alongside a user message like "Find engineers at Acme Corp." Print the raw model response. Confirm the model emits a structured tool call with the correct function name and arguments — not a prose answer.

2. **Implement a single-pass tool execution loop.** Building on Exercise 1, write code that detects when the model's response contains a tool call, extracts the function name and arguments, executes a mock `search_prospects` function (returning a hardcoded list of 2–3 prospects), appends the result as a `tool` role message, and sends the conversation back to the model. Print the model's final natural-language response. Verify that the model references the prospect data from the tool result rather than hallucinating.

3. **Compare `tool_choice` modes.** Write a script that sends the same user query ("What is 2 + 2?") to the API four times, varying only `tool_choice`: `auto`, `required`, `none`, and a specific function binding. For each mode, print the response type (text vs. tool_call) and the content. Before running, write your prediction for each mode as a comment. Run the script and verify whether your predictions match.

4. **Build a fault-tolerant tool execution loop.** Implement a multi-turn tool loop that handles three failure cases gracefully: (a) the model emits malformed JSON arguments, (b) the model calls a function name that does not exist in your tool registry, and (c) the target function raises a runtime exception (e.g., network timeout during an enrichment API call). For each failure, append an error message back into the conversation as a `tool` role result and let the model retry or respond. Your loop must survive at least 5 turns across a mix of successes and failures without crashing. Print a turn-by-turn log showing each tool call, its outcome, and the model's recovery behavior.

5. **Compute and trim tool schema costs.** Build a script that defines 6 tools (e.g., `search_prospects`, `enrich_company`, `find_email`, `score_lead`, `add_to_crm`, `schedule_followup`) with full JSON Schema definitions including descriptions and parameter docs. Use the `tiktoken` library to count the tokens consumed by the serialized tool definitions. Then produce a trimmed version: remove optional descriptions, shorten parameter names where safe, and eliminate any redundant nested schemas. Print the before-and-after token counts and the percentage reduction. Write the trimmed schema definitions to `signals/examples/trimmed_tools.py` as a importable module.

6. **Build a GTM enrichment agent.** Design and implement a multi-tool agent that takes a target company name and autonomously decides which tools to call to produce a lead profile. Your tool registry must include at least: a `search_company` tool (using the Apollo or Clearbit API, or a local mock returning structured company data), an `enrich_contact` tool (using a real enrichment API or a realistic mock), and a `write_to_crm` tool (posting to a HubSpot/Salesforce sandbox or writing to `outputs/skill-gtm-enrichment-result.md`). Run the agent against 3 sample companies. For each, print the full tool call sequence, arguments used, and final summary. The agent must use `tool_choice="auto"` and demonstrate at least one case where it skips a tool based on prior results. Write the implementation to `handlers/gtm_enrichment_agent.py`.
