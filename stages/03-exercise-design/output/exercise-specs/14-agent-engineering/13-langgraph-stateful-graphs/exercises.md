# Exercises — LangGraph: Stateful Graphs and Durable Execution

## Exercises

1. **Build a minimal `StateGraph` with typed state and a conditional edge.** Define a `TypedDict` state with keys `company_name` and `employee_count`. Create two nodes: `classify_company` (sets `tier` to `"smb"` if `employee_count < 200`, otherwise `"enterprise"`) and `route`. Add a conditional edge from `classify_company` that prints the tier and routes to `END`. Compile and invoke the graph with `{"company_name": "Acme Corp", "employee_count": 50}`. Your terminal must print the state dict at each node transition and the final state must contain `tier: "smb"`.

2. **Add a cycle to the graph from Exercise 1.** Introduce a `normalize_name` node that strips whitespace and title-cases `company_name`. Add a conditional edge from `normalize_name` that routes back to `classify_company` if the name changed, or to `END` if it did not. Invoke with `{"company_name": "  acme corp  ", "employee_count": 50}`. Your terminal output must show the graph visiting `normalize_name` → `classify_company` → `normalize_name` → `classify_company` → `END`, demonstrating the cycle fires exactly twice.

3. **Implement durable execution with a `MemorySaver` checkpointer.** Build a three-node graph (`enrich_firmographics` → `score_lead` → `write_result`) where `score_lead` raises a `RuntimeError` on its first invocation. Compile the graph with a `MemorySaver` checkpointer and a configurable thread ID. Invoke the graph, catch the error, then re-invoke with the same thread ID after patching the node to succeed. Print the checkpointed state before and after resumption. Your terminal must show the graph resuming from `score_lead`—not re-executing `enrich_firmographics`—proving the checkpoint restored mid-graph.

4. **Configure a retry loop with an attempt counter and threshold.** Build a graph with state keys `attempts` (int, starting at 0) and `api_result` (str). The single node `call_enrichment_api` increments `attempts` by 1 and randomly returns either `"success"` or `"error"`. A conditional edge routes back to `call_enrichment_api` if `api_result == "error"` and `attempts < 3`, otherwise routes to `END`. Run the graph 20 times. Print each run's final `attempts` count and confirm no run exceeds 3 attempts. Your terminal output must show at least one run that terminated at the threshold without succeeding.

5. **Build a multi-step enrichment waterfall that tracks cumulative API cost per node invocation.** Create a graph with four sequential enrichment nodes, each simulating a billed API call: `clearbit_firmographics` ($0.03), `apollo_contact_search` ($0.05), `hunter_email_verify` ($0.02), and `_clay_web_research` ($0.08). State includes `company_domain`, `enriched_data` (dict), `total_cost` (float), and `node_call_count` (dict mapping node name to int). Each node appends mock enrichment data, adds its cost to `total_cost`, and increments its call counter. Wrap the entire graph in a retry loop: if any node returns `status: "incomplete"`, the graph re-enters that node. Compile with a `MemorySaver` and run the graph on three real company domains (e.g., `"stripe.com"`, `"notion.so"`, `"gong.io"`). Write the final state for each company—including total cost, per-node call counts, and enriched data—to `outputs/skill-enrichment-waterfall.md` as a formatted Markdown table. Your terminal must print each node transition, its cost, and the running total. Verify that the artifact contains all three companies and that re-invocations (from retries) are reflected in the call counts.
