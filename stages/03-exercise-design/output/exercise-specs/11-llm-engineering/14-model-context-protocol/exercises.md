# Exercises — Model Context Protocol (MCP)

## Exercises

1. **Build a minimal MCP server and verify it with raw JSON-RPC.** Using the Python MCP SDK, implement a server that exposes a single tool called
