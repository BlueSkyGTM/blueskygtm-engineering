# Exercises — Positional Encoding â€” Sinusoidal, RoPE, ALiBi

## Exercises

1. **Implement sinusoidal positional encoding from scratch.** Compute a `PE` matrix of shape `(seq_len=8, d_model=8)` using the sin/cos formula at geometrically spaced frequencies. Print the full matrix rounded to 3 decimals. Verify that position 0 has all even-indexed (sine) entries equal to `0.000` and all odd-indexed (cosine) entries equal to `1.000`.

2. **Implement the ALiBi attention bias matrix.** For `n_heads=4` and `seq_len=10`, compute each head's slope as `slope[h] = 1 / 2^(2h+3)` and build a bias `bias[h, i, j] = slope[h] * (j - i)` for `j <= i`, else `-inf` (causal mask) or `0` (non-causal — your choice, state which). Print each head's `(10, 10)` bias matrix rounded to 3 decimals. Confirm that the diagonal `i == j` is `0.000` for every head and that the bias grows more negative with distance, with steeper slopes on earlier heads.

3. **Implement RoPE rotation and verify it encodes relative distance.** Take fixed random query `q` and key `k` vectors of dimension `d=8`. Implement the rotation `RoPE(x, pos)` that pairs dimensions `(2i, 2i+1)` and applies the angle `pos / 10000^(2i/d)`. Compute the dot product `<RoPE(q, i), RoPE(k, j)>` for all `i, j` in `range(0, 8)` and print the resulting `8×8` matrix. Verify that every cell on a constant `(i - j)` diagonal matches its neighbors to within `1e-5`, confirming the inner product depends on relative offset, not absolute position.

4. **Diagnose positional encoding in real production models and recommend one for a GTM task.** Load the HuggingFace config JSON for `meta-llama/Meta-Llama-3-8B`, `mistralai/Mistral-7B-v0.1`, and `mosaicml/mpt-7b` (use `AutoConfig.from_pretrained` or fetch the raw config). For each, inspect fields like `rope_theta`, `alibi`, `attention_impl`, `max_seq_len`, or `nope_impl` and classify the scheme as sinusoidal, RoPE, or ALiBi. Print a table: model name, detected scheme, max context length. Then, given a GTM task of scoring a 16k-token enrichment profile (Apollo company data + website scrape concatenated), print a recommendation for which model to deploy and one sentence on extrapolation reasoning.

5. **Build a persistent comparison artifact covering all three schemes on real GTM data.** Create `signals/examples/positional_comparison.py` that loads a Clay-enriched lead record (or a stub dict with fields `company_name`, `website`, `description`, `tech_stack`, `recent_news`), tokenizes the concatenated fields into a ~64-token sequence, and runs all three encoding schemes on it. The script must produce `outputs/skill-positional-encoding.md` containing: (a) a snippet of the sinusoidal `PE` matrix for positions 0–7, (b) the RoPE `8×8` relative-distance dot-product matrix for a sample query/key pair, (c) the ALiBi bias matrix for 4 heads at `seq_len=10`, and (d)
