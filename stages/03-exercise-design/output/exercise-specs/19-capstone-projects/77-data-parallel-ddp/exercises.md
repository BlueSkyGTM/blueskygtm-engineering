# Exercises — Data Parallel DDP From Scratch

## Exercises

1. **Implement process group initialization and parameter broadcast with `torchrun`.** Write a script that calls `init_process_group(backend="gloo")`, reads `RANK`, `WORLD_SIZE`, and `LOCAL_RANK` from the environment, initializes a small `nn.Linear(4, 2)` model with random weights on every rank, broadcasts rank 0's `state_dict` to all other ranks via a loop over `broadcast` on each parameter tensor, and prints `[rank N] param[0][0] = X` so you can visually confirm all ranks hold identical values after broadcast. Launch with `torchrun --nproc_per_node=2 your_script.py`.

2. **Implement manual gradient all-reduce with averaging and verify post-sync equality.** Create a 2-process script where each rank instantiates the same `nn.Linear(4, 2)`, runs a forward + backward pass on a *different* random input batch (use rank as a seed offset), then manually all-reduces each parameter's `.grad` using `all_reduce(op=ReduceOp.SUM)` followed by `.grad / world_size`. Print `[rank N] grad_norm BEFORE = X` and `[rank N] grad_norm AFTER = Y` for each rank. Confirm that all post-sync norms are identical across ranks but pre-sync norms differ.

3. **Verify that averaged gradients across shards equal the full-batch gradient.** Write a single-process script that computes the gradient of a `nn.Linear(10, 1)` model on a 64-example batch and saves the result. Then write a second script launched with `torchrun --nproc_per_node=4` where each rank computes the gradient on a disjoint 16-example shard, all-reduces with summation, divides by `world_size`, and prints the max absolute element-wise difference against the full-batch gradient (loaded from disk or recomputed on rank 0 and broadcast). Print `max_diff = X` — it should be on the order of floating-point epsilon.

4. **Implement manual DDP training for a lead-scoring model on enriched CRM data.** Download or create a CSV of 2,000 prospect records with columns like `employee_count`, `funding_total`, `email_opens_30d`, `page_visits_7d`, and `converted` (binary target). Write a `torchrun`-launchable training script with a `DistributedSampler`, a 2-layer MLP classifier, manual gradient all-reduce with averaging in the training loop, and per-epoch loss printed from each rank. Run with `--nproc_per_node=2` and confirm that both ranks report identical loss values after the first synchronized step.

5. **Benchmark scaling efficiency of your manual DDP implementation.** Extend Exercise 4 into a throughput benchmark: time 50 training steps at `world_size` = 1, 2, and 4 (run each separately via `torchrun --nproc_per_node=N`). Compute samples/second per rank, actual speedup relative to single-process, and ideal linear speedup. Write the results as a Markdown table with columns `world_size`, `samples/sec/rank`, `total_samples/sec`, `actual_speedup`, `ideal_speedup`, `efficiency_pct` to `outputs/skill-ddp-scaling-benchmark.md`. Include a one-paragraph diagnosis of whether the bottleneck is communication overhead, GIL contention, or data loading.

6. **Compare manual all-reduce against `nn.parallel.DistributedDataParallel` by gradient norm.** Implement the same lead-scoring
