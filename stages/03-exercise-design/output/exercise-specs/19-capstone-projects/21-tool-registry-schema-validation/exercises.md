# Exercises — Tool Registry with Schema Validation

## Exercises

1. Implement a `ToolRegistry` class with `register(name, handler, input_schema, output_schema)` and `call(name, arguments)` methods that validates input against `input_schema` before executing the handler and validates the handler's return against `output_schema` before returning it. Register a tool named `calculate_cagr` that accepts `start_value`, `end_value`, and `years` (all numbers, all required) and returns a dict containing `cagr` (number, required). Call it once with valid arguments and print the result. Then call it with a string where a number is expected and print the error message your registry raises.

2. Add output validation to your registry and demonstrate it failing on purpose. Register a tool `get_fiscal_year` whose input schema requires `year` (integer) and `quarter` (string, enum `["Q1", "Q2", "Q3", "Q4"]`). Define an output schema requiring `label` (string) and `summary` (string). Write the handler so it deliberately omits the `summary` field from its return dict. Call the tool through the registry, catch the output validation error, and print which required field was missing and at which pipeline stage the failure occurred.

3. Implement a `trace_call` method on your `ToolRegistry` that returns a structured execution log for any tool invocation. The log must be a dict containing `steps` (an ordered list of strings such as `"lookup"`, `"input_validated"`, `"executed"`, `"output_validated"` — or the step at which the call failed), `tool_name`, `success` (bool), and `result_or_error` (the return value or the exception message). Run `trace_call` against three scenarios: valid input with valid output, invalid input that should be rejected before execution, and valid input with invalid output. Print all three trace logs and confirm the `steps` list correctly reflects where each failure originated.

4. Register three enrichment-style tools that model real GTM data provider contracts. (a) `apollo_person_search` — input requires `email` (string, format `"email"`) with optional `api_key` (string); output requires `person` (object with `first_name`, `last_name`, `title`, `company_name`). (b) `clay_enrich_company` — input requires `domain` (string, matching a URL pattern); output requires `company` (object with `name`, `employee_count` as integer, `industry` as string). (c) `clearbit_company_lookup` — input requires `domain` (string); output requires `company` (object with `name`, `domain`, `metrics` as object). Write mock handlers that return hardcoded valid data for each. Call each tool with valid input and print the JSON response. Then call `clay_enrich_company` with `{"domain": "not_a_valid_domain"}` and print the validation error.

5. Build a comparison harness that demonstrates validated versus unvalidated tool execution side by side. Create a file `handlers/enrichment_comparison.py` containing a handler `handle_company_enrichment` with two intentional bugs: it returns `employee_count` as the string `"250"` instead of an integer, and it omits the `industry` field entirely. Write a function that (1) calls the handler directly without validation and prints the raw result, (2) registers the same handler in your `ToolRegistry` with an output schema requiring `company.employee_count` as integer and `company.industry` as string, calls it, and prints the validation error, and (3) prints a summary dict listing every failure the registry caught that the direct call
