# Exercises — Prompt Caching and Semantic Caching Economics

## Exercises

1. **Compute the break-even cache read count for L2 prompt caching.** Write a Python script that takes three inputs — base input price per MTok, cache write surcharge multiplier, and cache read discount multiplier — and outputs the minimum number of cache reads required to amortize a single cache write. Run it with Anthropic-tier pricing ($3.00/MTok base, 1.25× write surcharge, 0.10
