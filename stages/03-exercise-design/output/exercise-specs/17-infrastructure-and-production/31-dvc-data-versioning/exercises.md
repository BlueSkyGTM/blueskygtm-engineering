# Exercises — DVC: Data Version Control

## Exercises

1. **Initialize a DVC project and verify the pointer hash.** Create a new Git repository, install DVC, and run `dvc init`. Create a file `data/leads.csv` with at least five rows of mock prospect data (columns: `email`, `company`, `title`). Run `dvc add data/leads.csv`. Open the generated `data/leads.csv.dvc` file and extract the MD5 hash from the `outs` section. Compute the MD5 of the raw file independently using `md5sum data/leads.csv` (or `certutil` on Windows). Confirm the two hashes match. Also verify that `data/leads.csv` now appears in `.gitignore`.

2. **Configure a local DVC remote and round-trip the data.** Create a directory outside your project (e.g., `/tmp/dvc-remote` or `../dvc-storage`). Add it as a DVC remote with `dvc remote add`. Run `dvc push` and confirm that the hashed object appears under the remote directory. Delete the local cache (`rm -rf .dvc/cache`), then run `dvc pull`. Verify that `data/leads.csv` is restored and its checksum is unchanged. Run `dvc status -c` to confirm the remote and local cache are in sync — the output should report no missing or modified files.

3. **Build a two-stage pipeline that processes CRM export data.** Create a CSV file `data/crm_export.csv` with columns `contact_id`, `email`, `company`, `linkedin_url`, and at least 20 rows (some duplicate emails). Write a `dvc.yaml` with two stages: `clean` (depends on `data/crm_export.csv`, runs a Python script that deduplicates by email and writes `data/crm_clean.csv`) and `metrics` (depends on `data/crm_clean.csv`, counts unique companies and writes `metrics/scores.json`). Run `dvc repro` and confirm both outputs are created. Run `dvc status` to verify the pipeline is up to date. Use `dvc dag` to print the dependency graph and confirm the two stages are linked.

4. **Trace the `dvc.lock` hashes and verify each guarantee.** After running the pipeline from Exercise 3, open `dvc.lock`. For each stage entry, identify the `deps` and `outs` hashes. For each hash listed, manually compute the MD5 of the corresponding file on disk and confirm they match. Then modify `data/crm_export.csv` (add or remove a row) and run `dvc repro` again. Re-open `dvc.lock` and identify exactly which hash values changed. Write a brief summary (3–5 bullet points) explaining: which deps hash changed, which outs hash changed, why the `metrics` stage re-executed even though its script was untouched, and what the lock file would guarantee if a teammate cloned the repo and ran `dvc repro` on a clean machine. Print your summary to stdout.

5. **Compare two pipeline runs and produce a diff report.** Extend the pipeline from Exercise 3 with a third stage: `enrich` (depends on `data/crm_clean.csv`, runs a Python script that calls the Apollo person enrichment API — or a local mock if no API key is available — to append `company_headcount` to each row, writing `data/crm_enriched.csv`). Run `dvc repro`, commit everything to Git with tag `v1-baseline`. Then modify `crm_export.csv` to simulate a new weekly
